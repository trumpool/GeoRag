- README.md including:
    - Setup instructions (CPU-only path required)
      - I use uv to start a virtual environment, see the .venv file?
    - Model + embedding choices and rationale
    - Retrieval settings (k, chunk size, threshold)
    - Example outputs with citations
    - Mini evaluation table + analysis
    - Cost notes (confirm zero/near-zero)

